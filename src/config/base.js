'use strict';

// Settings configured here will be merged into the final config object.
const API_URL = '/json/';
export default {
  MAIN_CATEGORY: {
    path: 'main.json',
    name: 'Main Category'
  },
  API_PATH: {
    list: API_URL
  }
}
